---
description: 根据本次对话整理项目文档，并保存值得长期复用的知识
argument-hint: "[更新重点或补充要求]"
---

根据本次对话的内容更新以下文件，所有文件均不得超过 200 行。

需要更新的文件：

- `CLAUDE.md`：项目介绍、参考文件/代码库路径、当前项目进度、核心准则。
- `README.md`：面向使用者的说明和使用方法。
- 记忆文件: 使用`memory` 工具保存经过验证、可跨会话复用的知识。如果不存在值得长期保存的内容，不要为了完成步骤而创建记忆。

> $ARGUMENTS